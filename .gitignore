# 操作系统和IDE配置文件
.DS_Store
.vscode/
.idea/

# 环境变量文件
# 忽略所有.env文件，但保留示例文件
.env
.env.*
!.env.example
backend/.env

# 日志文件
*.log
logs/
*.log.*

# Python / 后端
__pycache__/
*.py[cod]
*.egg-info/
.venv
venv/
env/
pip-wheel-metadata/

# 后端数据库和运行时文件
*.db
# (removed invalid entry) backend/app.main:app
# (removed invalid entry) backend/uvicorn

# Node / 前端
frontend/node_modules/
frontend/build/
frontend/coverage/
frontend/.eslintcache
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.aider*

# Benchmarks caches (root and backend)
.benchmarks/
backend/.benchmarks/

# mypy caches (root and backend)
.mypy_cache/
backend/.mypy_cache/

# Ruff caches (root and backend)
.ruff_cache/
backend/.ruff_cache/

# Pytest caches (root and backend)
.pytest_cache/
backend/.pytest_cache/

# Coverage artifacts
.coverage
htmlcov/
coverage.xml

# Coverage parallel-mode shards
.coverage.*
